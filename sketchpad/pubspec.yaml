name: sketchpad
description: A HiveDB sample project.

publish_to: "none"

version: 1.0.0

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  hive: any
  hive_flutter: any
  path_provider: any

flutter:
  uses-material-design: true

  fonts:
    - family: OpenSans
      fonts:
        - asset: fonts/OpenSans-Regular.ttf

name: counter
description: This is a simple counter app meant to demo Hive's persistence and speed.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0

environment:
  sdk: '>=2.12.0'

dependencies:
  flutter:
    sdk: flutter

  hive: any
  hive_flutter: any

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true

name: todo
environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  hive: ^1.1.1
  hive_flutter: any
  path_provider: ^1.4.0

dev_dependencies:
  hive_generator: any
  build_runner: ^1.7.1

flutter:
  uses-material-design: true

  fonts:
    - family: OpenSans
      fonts:
        - asset: fonts/OpenSans-Regular.ttf
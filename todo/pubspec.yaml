name: todo
environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  hive: ^1.2.0-dev
  hive_flutter: any
  path_provider: any

dev_dependencies:
  hive_generator: any
  build_runner: any

flutter:
  uses-material-design: true

  fonts:
    - family: OpenSans
      fonts:
        - asset: fonts/OpenSans-Regular.ttf